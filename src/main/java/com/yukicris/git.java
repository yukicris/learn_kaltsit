package com.yukicris;

public class git {
    //cherry pick 成功后,未提交想撤回
    //$ git reset --hard HEAD^

    //如果本地代码有变化的话
   /* $ git stash
    $ git reset --hard HEAD^
    $ git stash pop  # or `git stash apply`, if you want to keep the changeset in the stash*/
}
