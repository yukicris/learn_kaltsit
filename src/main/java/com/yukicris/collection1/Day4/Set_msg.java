package com.yukicris.collection1.Day4;

public class Set_msg {
    // set基本介绍
    /**
     * 无序,添加和取出顺序不一致,没有索引
     * 不允许重复元素,最多包含一个null
     * 主要是hashset,treeset 等等
     *
     * Set接口常用方法,和List一样,也是Collection的子接口,因此,常用的方法和Collection一样
     *
     * Set接口的遍历方式
     * 同Collection的遍历方式一样,因为Set接口是Collection接口的子接口,
     * 所以可以使用迭代器
     * 增强for
     * 不能使用索引的方式进行获取,没用提供get方法
     *
     * HashSet实际上是HashMap
     * public HashSet(){
     *     map = new HashMap<>();
     * }
     *
     * 可以存放null值,但是只能有一个null
     * HashSet不保证元素是有序的,取决于Hash后,再确定索引的结果
     * 不能有重复元素/对象
     *
     *
     */
}
